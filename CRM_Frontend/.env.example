# Local Next.js (default points at Django on :8000)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000

# Production Docker build uses empty values (same-origin via nginx) — see Dockerfile
