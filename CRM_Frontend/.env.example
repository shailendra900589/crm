# Local Next.js — points at Django on this machine
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000

# Production Docker build uses: same-origin  (see CRM_Frontend/Dockerfile)
# Browser then calls https://crm.trackbook.co/api/... via nginx
